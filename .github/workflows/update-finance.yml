name: Update Campaign Finance (Maine FEC)
# DISABLED — This workflow previously pulled California CAL-ACCESS data.
# Finance data for Maine 2026 Senate race is sourced directly from FEC.gov
# and stored in data/ JSON files. To refresh, run the FEC API scripts manually.
#
# on:
#   schedule:
#     - cron: '0 7 * * *'
#   workflow_dispatch: {}

on:
  workflow_dispatch:
    inputs:
      confirm:
        description: 'Type ENABLE to manually trigger (disabled by default)'
        required: true

permissions:
  contents: write

jobs:
  disabled:
    runs-on: ubuntu-latest
    steps:
      - name: Workflow disabled
        run: |
          echo "This workflow is disabled. Finance data comes from FEC.gov JSON files."
          echo "See data/campaign-*.json for Maine 2026 Senate race data."
          exit 0
